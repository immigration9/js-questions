const assert = require('assert');

/**
 * Basic Calculations.
 * @param {number} a
 * @param {number} b
 * a, b 숫자가 매개변수로 주어지는 함수가 제공되어 있다
 * 각각 사칙연산을 수행하는 함수들을 구현하시오
 */

function add(a, b) {
  /** implement add function */
}

function subtract(a, b) {
  /** implement subtract function */
}

function multiply(a, b) {
  /** implement multiply function */
}

function divide(a, b) {
  /** implement divide function */
}

/**
 * DO NOT MODIFY TEST CASES.
 */
assert.equal(add(3, 5), 8);
assert.equal(add(5, 12), 17);
assert.equal(subtract(10, 5), 5);
assert.equal(subtract(8, 2), 6);
assert.equal(multiply(3, 5), 15);
assert.equal(multiply(10, 3), 30);
assert.equal(divide(10, 2), 5);
assert.equal(divide(100, 20), 5);

console.log(`
  ** TEST FINISHED: if you are seeing this message,
  you can proceed to the next step
`);
